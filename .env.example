# EXPRESS AUTH API - Environment Variables
# ===========================================

# ===== REQUIRED VARIABLES =====
# These MUST be set or the application will not start

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/express-auth-api
MONGODB_URI_PROD=mongodb+srv://username:password@cluster.mongodb.net/express-auth-api

# JWT Configuration (minimum 32 characters for security)
JWT_SECRET=your-super-secret-jwt-key-here-make-it-at-least-32-characters-long

# Session Configuration (minimum 32 characters for security)
SESSION_SECRET=your-super-secret-session-key-here-make-it-at-least-32-characters-long

# Email Configuration (Resend API)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxx

# ===== OPTIONAL VARIABLES =====
# These have default values but can be customized

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your-super-secret-refresh-jwt-key-here-make-it-different-from-jwt-secret
JWT_REFRESH_EXPIRES_IN=7d
SESSION_EXPIRES_IN=30d

# Email Configuration (Optional)
FROM_EMAIL=onboarding@resend.dev
APP_NAME=Express Auth API
FRONTEND_URL=http://localhost:3000

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging Configuration
LOG_LEVEL=info
LOG_FILE=logs/app.log

# Security Configuration
BCRYPT_SALT_ROUNDS=12
MAX_LOGIN_ATTEMPTS=5
LOCK_TIME=7200000

# CORS Configuration (comma-separated list)
CORS_ORIGIN=http://localhost:3000

# ===== PRODUCTION EXAMPLE =====
# For production, use strong secrets and appropriate URLs:
# JWT_SECRET=generate-a-strong-random-string-with-at-least-64-characters-for-production
# SESSION_SECRET=generate-another-strong-random-string-with-at-least-64-characters
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database
# CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
# NODE_ENV=production
